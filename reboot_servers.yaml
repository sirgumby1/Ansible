---
- name: win_reboot module
  hosts: all
  become: false
  gather_facts: false
  tasks:
    - name: reboot host(s)
      win_reboot:
        msg: "reboot by Ansible"
        pre_reboot_delay: 60
        post_reboot_delay: 10
